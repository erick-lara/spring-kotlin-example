spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:kotlin-ex-db
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true